<%= form_for(@eavesdrop) do |f| %>
  <% if @eavesdrop.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@eavesdrop.errors.count, "error") %> prohibited this eavesdrop from being saved:</h2>

      <ul>
      <% @eavesdrop.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :initiator_id %><br>
    <%= f.number_field :initiator_id %>
  </div>
  <div class="field">
    <%= f.label :responder_id %><br>
    <%= f.number_field :responder_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
