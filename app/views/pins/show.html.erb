<%= link_to 'Back', pins_path %>
<%= @pin.responder.name %>

<div class="row">
	<div class="col-md-offset-2 col-md-8">
		<div class="panel panel-default">
			<div class="panel-heading center">
				<%= image_tag @pin.image.url(:medium) %>	
			</div>
			<div class="panel-body">
				<p><%= @pin.description %></p>
        <p><%= @pin.message1 %></p>
        <p><%= @pin.message2 %></p>
        <p><%= @pin.message3 %></p>
        <p><%= @pin.message4 %></p>
        <p><%= @pin.message5 %></p>
        <p><%= @pin.message6 %></p>
				<p><strong><%= @pin.user.name if @pin.user %></strong></p>
				<% if @pin.user == current_user %>
					<%= link_to edit_pin_path(@pin) do %>
            <span class="glyphicon glyphicon-edit"></span>Edit
          <% end %>
				<% end %> 
			</div>
		</div>
	</div>
</div>

<%# @conversation_items = @pin.initiator.conversation_list(@pin.initiator, @pin.responder) %>
<%# This is to list out the conversation between users %>
<section>
  <%= render 'users/conversation', :collection => @conversation_items %>
</section>

<div class="span8">
  <%#= render 'microposts/communicate_form' %>
</div>
