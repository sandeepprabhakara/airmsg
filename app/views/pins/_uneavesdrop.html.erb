<%= form_for(current_user.eavesdrops.find_by(user_id: current_user.id, initiator_id: @pin.initiator_id, responder_id: @pin.responder_id), html: { method: :delete }) do |f| %>
  <%= f.submit "Uneavesdrop", class: "btn btn-large  btn-primary" %>
<% end %>